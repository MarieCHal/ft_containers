#ifndef REVERSE_ITERATOR_HPP
# define REVERSE_ITERATOR_HPP

/**
 * reverse_iterator reverses the direction of a given iterator
 * moves from the end to the begining
 * */

namespace ft
{
    template <class Iter>
    class reverse_terator
    {
        
    };
}

#endif